# 数据库综合测试

## 考试信息

- **总分**：100分
- **考试时间**：2小时

## 第一部分：选择题（每题 3 分，共 30分）

1. 关于MySQL事务的ACID特性，以下描述错误的是：

A. 原子性保证事务中的所有操作要么全部完成，要么全部不完成

B. 一致性确保事务执行前后数据库从一个一致状态转换到另一个一致状态

C. 隔离性保证并发事务之间相互不影响

D. 持久性确保事务提交后，对数据库的修改立即永久保存到磁盘

1. 在MySQL中，关于索引的说法正确的是：

A. 主键索引和唯一索引没有区别

B. 联合索引中字段的顺序不影响查询性能

C. 表中有索引就一定能提高查询性能

D. 使用LIKE '%keyword'查询时无法使用索引

1. 关于数据库三范式，以下描述正确的是：

A. 第一范式要求每个字段都是原子性的

B. 第二范式要求非主键字段完全依赖于主键

C. 第三范式要求消除传递依赖

D. 以上都正确

1. 在JDBC中，PreparedStatement相比Statement的主要优势不包括：

A. 防止SQL注入攻击

B. 提高执行效率

C. 支持批量操作

D. 简化SQL语句编写

1. 关于数据库连接池，以下说法错误的是：

A. 连接池可以减少创建连接的开销

B. 连接池中的连接可以重复使用

C. 连接池可以设置最大连接数

D. 连接池不需要考虑并发问题

1. 关于数据库连接池，以下哪个配置可能导致连接泄露？

A. maxTotal=50, maxIdle=20, minIdle=5

B. testOnBorrow=true, validationQuery="SELECT 1"

C. removeAbandonedTimeout=300, removeAbandonedOnBorrow=true

D. maxWait=5000, testWhileIdle=false

1. 关于InnoDB的行级锁，以下说法正确的是：

A. 行锁是在索引记录上加的锁

B. 如果没有索引，UPDATE操作会锁住整张表

C. 间隙锁只存在于REPEATABLE-READ隔离级别

D. 以上都正确

1. 关于MySQL的REPEATABLE-READ隔离级别，以下说法错误的是：

A. 可以防止脏读和不可重复读

B. 使用MVCC机制实现

C. 是MySQL InnoDB的默认隔离级别

D. 完全解决了幻读问题

1. 在JDBC编程中，关于ResultSet的处理，以下哪种做法是错误的？

A. 使用try-with-resources语句自动关闭ResultSet、Statement和Connection

B. 通过ResultSet.getMetaData()来动态获取查询结果的信息

C. 使用while(rs.next())来遍历结果集

D. 在遍历ResultSet的同时，使用同一个Statement对象执行新的查询

1. 关于MySQL的MVCC（多版本并发控制）机制，以下说法错误的是：

A. MVCC通过为每个事务生成数据快照来实现非阻塞读操作

B. 在REPEATABLE-READ隔离级别下，事务开始时读取的数据快照在整个事务期间保持不变

C. 每条记录都会包含创建版本号和删除版本号字段

D. MVCC完全避免了所有的锁竞争，大幅提升了系统并发性能

## 第二部分：SQL编程题（共40分）

**数据库表结构设计（10分）**

设计一个简单的电商系统数据库，包含以下实体：

- 用户（用户ID、用户名、邮箱、注册时间、最后登录时间）
- 商品（商品ID、商品名称、价格、库存、上架时间）
- 订单（订单ID、用户ID、订单金额、下单时间、订单状态）
- 订单明细（明细ID、订单ID、商品ID、购买数量、商品单价）

要求：

1. 写出建表语句，包含适当的数据类型和约束
2. 设计合理的主键和外键关系

**SQL查询题（每题6分，共30分）**

使用上述设计的电商数据库，编写SQL语句：

1. 查询2023年每个月的订单总金额，按月份升序排列
2. 查询购买金额前5名的用户信息（用户名、总购买金额）
3. 查询从未下过单的用户列表
4. 查询库存紧张（库存少于10）且近30天没有销售记录的商品
5. 使用事务完成以下操作：用户(ID=1001)购买商品(ID=2001) 2件，需要更新商品库存，生成订单和订单明细记录（**使用 JDBC 代码完成**）

## 第三部分：JDBC编程题（共30分）

**JDBC基础编程（15分）**

编写一个Java方法，实现以下功能：

```java
/**
 * 根据用户ID查询用户订单信息
 * @param userId 用户ID
 * @return 包含订单基本信息和订单明细的Map列表
 */
public List<Map<String, Object>> getUserOrders(int userId) {
    // 实现代码
    // 要求：
    // 1. 使用PreparedStatement防止SQL注入
    // 2. 合理处理数据库连接（使用try-with-resources）
    // 3. 正确处理异常
    // 4. 返回的数据应包含：订单ID、订单金额、下单时间、订单状态、商品名称、购买数量、商品单价
}
```

---

**JDBC高级编程（15分）**

编写一个Java方法，实现商品库存管理：

```java
/**
 * 批量更新商品库存
 * @param productUpdates Map<商品ID, 库存变化量>（正数表示入库，负数表示出库）
 * @return 成功更新的商品数量
 * @throws SQLException 
 */
public int batchUpdateStock(Map<Integer, Integer> productUpdates) throws SQLException {
    // 实现代码
    // 要求：
    // 1. 使用事务确保数据一致性
    // 2. 使用批量操作提高性能
    // 3. 在更新前检查库存是否充足（出库时不能导致库存为负数）
    // 4. 返回实际成功更新的商品数量
}
```