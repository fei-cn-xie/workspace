## python 虚拟环境管理

```shell
# 创建虚拟环境（env 是环境名称，可自定义）
python -m venv env


# 删除虚拟环境
# Windows (cmd/powershell)
rmdir /s env

# 或者使用 del
rm -rf env

# macOS/Linux
rm -rf env

```

## Git相关

### git tag
```sh
git tag -a tag_name -m "tag 相关信息"

git push origin --tags

```


